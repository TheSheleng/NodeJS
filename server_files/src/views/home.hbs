<h2>Home Page</h2>

{{#if user}}
  <p>Welcome, {{user.login}}!</p>
  {{#if user.image}}
    <img src="/{{user.image}}" width="50px" height="50px" alt={{user.image}} />
  {{else}}
    <p>No profile picture available</p>
  {{/if}}
{{else}}
  <p>Please log in to access your profile.</p>
{{/if}}
